from Bot import Bot


class BotGetWeapon(Bot):
    x : list = [(ea, Bot), ..]

    def play_single_turn(self, current_game_state, current_map, self_info, other_info):
        for i in x:
            if i[0](...):
                not i[1].play_sin
                break;
